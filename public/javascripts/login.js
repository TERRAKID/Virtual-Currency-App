const baseUrl="http://localhost:3000",btnLogin=document.querySelector(".btn--login"),alert=document.querySelector(".alert"),alertText=document.querySelector(".alert__text");btnLogin.addEventListener("click",()=>{let e=document.querySelector("#email").value,t=document.querySelector("#password").value;fetch(baseUrl+"/users/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:e,password:t})}).then(e=>e.json()).then(e=>{if("success"===e.status){let t=e.data.token;localStorage.setItem("token",t),window.location.href="/"}else alertText.textContent="Whoops, something went wrong. We couldn't log you in.",alert.classList.remove("hidden")}).catch(e=>{alertText.textContent="Whoops, something went wrong. We couldn't log you in.",alert.classList.remove("hidden")})});